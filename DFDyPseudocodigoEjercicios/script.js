// Crear una función calcularAreaTriangulo que tome como argumentos la base y la altura de un triángulo y devuelva el área del mismo

// calcularAreaTriangulo(base, altura)

// calcularAreaTriangulo(3, 4) // 6
// calcularAreaTriangulo(5, 6) // 15

// function calcularAreaTriangulo (base,altura){
//     let area = (base*altura)/2;
//     return area
// }
// calcularAreaTriangulo(10,12);
// document.write('el area del triangulo es de ' + calcularAreaTriangulo(10,12))


// const calcularAreaTriangulo = () => {
//     const base = parseInt(prompt('ingrese la base de un triangulo'));
//     const altura = parseInt(prompt('ingrese la altura de un triangulo'));
//     let area = (base*altura)/2
//     document.write(`${area}`)
// }
// calcularAreaTriangulo();


//******************************************************************************* */

// Conversión USD

// Una empresa importadora desea determinar cuántos dólares puede adquirir con determinada cantidad de pesos. Realizá un diagrama de flujo y pseudocódigo que representen el algoritmo para tal fin.
// const cantidadDeDolares = () => {
//     let precioDolar = parseInt(prompt('ingrese el precio en pesos del dolar'));
//     let pesosDeLaEmpresa = parseInt(prompt('ingrese la cantidad de pesos que tiene la empresa'));
//     let compra = (pesosDeLaEmpresa/precioDolar)
//     document.write(`la cantidad de dolares que la empresa puede comprar es de ${compra} dolares`)
// }
// cantidadDeDolares();

//******************************************************************************* */

// Edad

// Una empresa que contrata personal requiere determinar la edad de las personas que solicitan trabajo, pero cuando se les realiza la entrevista sólo se les pregunta el año en que nacieron. Realizá el diagrama de flujo y pseudocódigo que representen el algoritmo para solucionar este problema.